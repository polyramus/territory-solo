# M0 Tasks: Scaffolding and the Seam

- [x] 1. Record confirmed stack in DECISIONS.md
  - Stack is already confirmed by manifest.json (Unity 6, OpenXR, meta-openxr 2.5.0, MRUK 203.0.0)
  - Add DECISIONS.md entry: Unity 6000.x, OpenXR backend, no legacy Oculus XR Plugin
  - Confirm target device: Quest 3-class (color passthrough)
  - _Requirements: REQ-M0-6_

- [x] 2. Create three assembly definitions with enforced dependency direction
  - Create `Assets/Scripts/Core/Game.Core.asmdef` with no rendering/XR assembly references
  - Create `Assets/Scripts/Presentation/Game.Presentation.asmdef` referencing Game.Core
  - Create `Assets/Scripts/App/Game.App.asmdef` referencing Game.Core and Game.Presentation
  - Verify enforcement: temporarily add a UnityEngine.Rendering using statement to a Core file and confirm compile error
  - _Requirements: REQ-M0-1_

- [x] 3. Set up test infrastructure (see testing-strategy spec tasks 1, 2, 5, 6, 7)
  - Create `Game.Core.Tests` EditMode assembly and `Game.Presentation.Tests` PlayMode assembly
  - `SimulationRunner` and `TestFixtures` (testing-strategy tasks 3–4) moved to M1 — they
    depend on `Axis6` and the M1 `TickSimulation` constructor and cannot be built here
  - [x] Add `IPlayVolumeSource` interface (`Assets/Scripts/Presentation/XR/IPlayVolumeSource.cs`)
  - [x] Add `MRUKPlayVolumeSource` implementation (`Assets/Scripts/Presentation/XR/MRUKPlayVolumeSource.cs`)
  - [x] Create `Assets/Tests/Rooms/TestRoom.json` room fixture
  - [x] Create `runtests.ps1` at project root
  - [x] Write smoke test `CoreAssemblyLoads`: instantiate `TickSimulation`, assert non-null
  - [ ] Confirm: `.\runtests.ps1 -mode editmode` exits 0 (requires Unity running)

- [x] 4. Implement GameState and serialization round-trip
  - Create `GameState.cs` in Game.Core with `[Serializable]` and `tick` field
  - Write edit-mode test: serialize `GameState` to JSON → deserialize → assert `tick` matches
  - _Requirements: REQ-M0-5_

- [x] 5. Implement fixed-step tick loop skeleton
  - Create `TickSimulation.cs` in Game.Core with `Tick()` incrementing `State.tick`
  - Write edit-mode test: call `Tick()` N times, assert `State.tick == N`
  - _Requirements: REQ-M0-5_

- [x] 6. Set up passthrough scene (OpenXR / AR Foundation — no OVRPassthroughLayer)
  - [x] Enable Camera (Passthrough) feature in OpenXR Package Settings for Android (`m_enabled: 1`)
  - [x] Create `Assets/Editor/CreateMainScene.cs` — Editor menu generates the full scene:
    - XROrigin with Camera Offset → Main Camera (ARCameraManager + ARCameraBackground)
    - Controller placeholders (Left/Right) for M1 input binding
    - MRUK prefab instantiation from package
    - Play Volume Root with wireframe boundary geometry and tinted North face (-X)
    - Game Manager with PlayVolumeAnchor wired to Play Volume Root
  - [ ] Run `Tools → Voxel Territory → Generate Main Scene` in Unity Editor
  - [ ] Build and deploy to Quest 3; verify stable color passthrough on launch
  - _Requirements: REQ-M0-2_

- [x] 7. Anchor the play volume via MRUK (no OVRSpatialAnchor)
  - [x] Create `IPlayVolumeSource` interface — abstraction for testability (not direct MRUK dependency)
  - [x] Create `MRUKPlayVolumeSource` — wraps `MRUK.Instance.LoadSceneFromDevice()` and exposes `FloorCenter`
  - [x] Implement `PlayVolumeAnchor` MonoBehaviour in Game.Presentation/XR/:
    - Depends on `IPlayVolumeSource`, polls `IsReady`, calls `ApplyAnchor(floorCenter)`
    - Configurable dimensions (default 2m × 2m × 2m)
    - Positions Play Volume Root at floor center + half-height
  - [x] Boundary geometry generated by CreateMainScene.cs:
    - 12 wireframe edges (cyan for X/Z, green for Y axis)
    - Tinted blue face on -X boundary marking "North" (world-frame legibility per D5)
  - [ ] Deploy and verify: volume is stable, does not drift, placeholder visible, fits guardian
  - _Requirements: REQ-M0-3_

- [ ] 8. Verify all M0 exit criteria
  - [ ] App launches on-device into stable anchored passthrough with an empty volume visible
  - [ ] Game.Core compiles with no rendering/XR dependency
  - [ ] `.\runtests.ps1 -mode editmode` exits 0 (smoke test + serialization test pass)
  - [ ] State snapshot round-trips in a passing edit-mode test
  - [ ] Platform, XR stack, and device decisions recorded in DECISIONS.md
